# 지시문 No.013 완료 보고

작업일: 2026-09-04 · baseline `af12da9` → 최종 `main`

---

## 1. 작업 결과: **PASS**

성공 기준 16개를 모두 충족했다.

## 2. KRISS 최종 판정

「중적외선 레이저 시스템」(공고 제26-01-038-01호) → **제외**

- 판정 사유: `장비(시스템)이지만 반도체/디스플레이/TGV 공정·산업 근거 없음 — 일반 연구·계측 장비`
- 근거 문서: `docs/REVIEW-KRISS-63846.md` (공개 규격서 확인 내역)

## 3. 최종 건수

| | Before | After |
|---|---|---|
| 전체 | 67건 | **67건** |
| 장비 | 45건 | **45건** |
| 검토 필요 | 1건 | **0건** |
| 제외 | 21건 | **22건** |

기존 67건 기준 변경은 KRISS 1건뿐이고 **신규 유입은 0건**이다(Actions 2회
실행 모두 신규 공고 없음). 두 축이 섞이지 않았다.

## 4. KRISS 판정 규칙 일반화

`equipment_filter.py`의 "산업 신호 없음 + 장비 신호 있음" 분기를
`검토 필요` → `제외`로 바꿨다. 특정 공고 ID를 예외 처리하지 않았다.

- 산업 근거는 `INDUSTRY_TERMS`(반도체·웨이퍼·디스플레이·패널·TGV·유리기판·
  패키징 등)와 `PROCESS_INDUSTRY_TERMS`(식각·증착·노광·CMP·AOI·프로버·
  성장로·박막 등)로 넓게 잡혀 있다. 실제 반도체 장비 공고는 제목이나 본문에
  이 중 하나가 거의 반드시 들어간다.
- 기관·부서명은 산업 근거로 쓰지 않는다(No.009에서 확정한 규칙 유지).
- 본문(`description`)에 산업 근거가 있으면 인정한다 — 제목만 보고 과잉
  제외하지 않기 위해서다.

**전수 회귀검증**: 67건을 재판정한 결과 판정이 바뀐 것은 KRISS 1건뿐이다.
MOCVD·lithography·ALD·wet etch·AOI·tester·prober·inspection·metrology·
성장로 공고는 전부 `장비`를 유지했다. 회귀테스트에 포함 12종과
연구계측장비 제외 6종을 고정했다.

**부수 발견 — 기존 오제외 버그 1건 수정.** `FACILITY_TERMS`의 `"모니터"`가
한글 부분 문자열 매칭 때문에 **"모니터링"**에 걸려, "식각 공정 모니터링 장비"
같은 정상 공고가 팹 시설·유틸리티로 제외되고 있었다. 현재 67건에 해당 공고가
없어 드러나지 않았을 뿐이다. 제거하고 회귀테스트로 고정했다(사무용 모니터
구매는 산업·장비 신호가 없어 여전히 제외된다).

## 5. 중국어 로마자 잔여 Before / After

| | Before | After |
|---|---|---|
| 로마자 잔여 공고 | **8건** | **1건** |
| `translationIncomplete` | 22건 | **17건** |
| MOFCOM 로마자 폴백 | 5건 | **0건** |

남은 1건은 `YiSi Wei`(奕斯伟) — **회사명**이다. 지시문 7번대로 근거 없이
한국어로 창작하지 않고 그대로 뒀다.

Before의 8건 중 3건은 애초에 결함이 아니었다 — `Shennan Circuits`,
`Xinlian Microelectronics`, `Shaanxi`는 괄호 안 공식 영문명이고 `D-Poly`는
제품·공정명이다.

## 6. 추가한 중국어 용어집

실제 운영 제목에 로마자로 남아 있던 것만 넣었다.

| 원문 | 번역 | 로마자였던 형태 | 출현 |
|---|---|---|---|
| 技术 | 기술 | `JiShu` | 3회 |
| 产线 | 생산라인 | `ChanXian` | 2회 |
| 光电 | 광전 | `GuangDian` | 2회 |
| 显示 | 디스플레이 | `XianShi` | 1회 |
| 印刷 | 인쇄 | `YinShua` | 1회 |
| 高清 | 고화질 | `GaoQing` | 1회 |
| 一期 | 1단계 | `YiQi` | 1회 |
| 板级封装 | 패널레벨 패키징 | `WeiBan Ji` | 1회 |
| 숫자+年 | 숫자+년 | `Nian` | 2회 |

주의해서 처리한 것:

- **`产线`은 `生产线`과 같은 사전에 넣어야 한다.** 다른 사전에 두면 짧은
  쪽이 먼저 치환되어 `OLED生产线`이 `OLED生 생산라인`으로 깨진다.
- **`一期`는 "1기"가 아니라 "1단계"다.** 1차 실행에서 "1기"로 넣었더니
  번역 모델이 이미 쓰던 "1단계"를 덮어썼다. 기존 번역이 더 자연스러워
  그쪽에 맞췄다.
- **연도·세대 패턴은 공백을 함께 낸다.** 용어집이 한자를 많이 흡수할수록
  번역 모델이 띄어쓰기를 넣어 줄 기회가 줄어, `2026년제4.5세대TFT-LCD`처럼
  붙어 버렸다. 1차 실행에서 발견해 고쳤다.

번역 엔진은 교체하지 않았고 `translation_memory`도 초기화하지 않았다.

## 7. 기존 번역 변경 건수

| 실행 | title | org | 성격 |
|---|---|---|---|
| 1차 | 9건 | 5건 | 개선 7 + **회귀 2** |
| 2차 | 10건 | 0건 | 회귀 2건 복구 + 띄어쓰기 개선 3건 |

1차의 회귀 2건(`1단계`→`1기`, 띄어쓰기 소실)은 6절대로 수정해 2차에서 복구를
확인했다. **최종적으로 나빠진 번역은 0건**이며, 용어집과 무관한 JETRO·국내
수집원 제목은 한 건도 바뀌지 않았다.

2차에서는 `제8.6세대AMOLED` → `제8.6세대 AMOLED`처럼 **원래부터 붙어 있던
제목 3건**도 함께 개선됐다.

## 8. EBNEW URL drift

`http://` 잔존 **0건**(`url`·`originalUrl` 모두). Actions 2회 실행 후에도
URL 필드 변경 0건 — 스킴 흔들림이 재발하지 않았다.

## 9. `check_data_drift.py` 감사 결과

상세는 `docs/AUDIT-No013.md`. 요약:

- 워크플로 순서는 **이미 안전**해 변경이 필요 없었다.
- CASE A~E 전부 차단, CASE F 통과 — 실제 스크립트로 실측.
- 이번에 **CASE E(기존 ID 대량 유실)** 검사를 새로 추가했다. 건수만 보면
  잡히지 않는 유형이다.
- 급감 기준을 상수로 분리해 현재 건수(67)에 의존하지 않게 했다.

## 10. Workflow 실제 실행 순서

```
수집(Fetch) → 데이터 이상 변동 검사 → Telegram(신규) → Telegram(장애/복구) → git commit/push
```

워크플로에 `if:` 조건이 하나도 없고 알림·커밋 스텝에 `continue-on-error`가
붙어 있지 않다. GitHub Actions는 실패한 스텝에서 잡을 중단하므로 **drift 실패
시 Telegram 2종과 git add/commit/push가 아예 실행되지 않는다.**

## 11~14. 보호장치 테스트 결과

| CASE | 상황 | 결과 | 검출 |
|---|---|---|---|
| A | 빈 파일 | **FAIL** | `가 비어 있습니다` |
| B | JSON 파싱 불가 | **FAIL** | `Expecting value: line 1 column 20` |
| C | 67 → 3건 급감 | **FAIL** | 건수·ID·장비 판정 3중 검출 |
| D | firstSeenAt 대량 유실 | **FAIL** | `67건의 firstSeenAt이 사라졌습니다` |
| E | ID 대량 유실(건수 동일) | **FAIL** | `67건 중 0건만 남았습니다 (0%)` |
| F | 정상 1건 변화 | **PASS** | ID 99% 생존 |

급감 기준은 비율(직전 대비 절반 미만) + 절대 하한(`SMALL_DATASET_FLOOR = 10`,
`ID_SURVIVAL_MIN = 0.30`)이다. 300건 → 280건은 통과하고 300건 → 20건은
차단되는 것을 회귀테스트로 고정했다.

## 15. Telegram 보호 결과

drift 검사가 알림보다 앞이므로 CASE A~E에서 **알림이 나가지 않는다.**
장애/복구 알림도 손상된 데이터를 근거로 잘못 발생하지 않는다 — 같은
`announcements.json`을 읽는 스텝이 모두 drift 뒤에 있기 때문이다.

## 16. commit/push 보호 결과

`git add`·`commit`·`push`가 전부 마지막 스텝 하나에 들어 있고 drift보다
뒤에 있다. drift 실패 시 세 명령 중 **어느 것도 실행되지 않는다.**

## 17. 자동 테스트

**71건 / PASS 71 / FAIL 0** (55건 → 71건, +16건)

추가한 것: 연구계측장비 제외 5종, 반도체 계측장비 포함 6종, KRISS 실제 공고,
모니터링 오제외 방지, 중국어 용어집 8종 + 연도·띄어쓰기·사전 순서·고유명사,
drift CASE E·부분 교체 허용·소규모 표본·건수 비의존.

## 18. No.011 / DGIST 설명 정정: **완료**

`docs/OPERATIONS.md` 10절에 정확한 내용을 적었다.

- DGIST의 `Connection reset`은 `<urlopen error ...>` 형태, 즉 urllib이
  `URLError`로 감싼 것이라 **기존 코드에서도 재시도됐다.**
- No.011이 한 일은 ① `URLError`로 감싸지지 않는 예외(`RemoteDisconnected` 등)
  까지 재시도 범위를 넓힌 것, ② 그 규칙을 9개 수집기에 표준화한 것이다.

## 19. KOTRA 문서 표현 정정: **완료**

정정 후 표현은 **"러너 IP에 따른 선택적·부분적 접속 차단"**이다.

| 파일 | Before | After |
|---|---|---|
| `README.md` | "Actions 러너에서는 TCP 443 연결 자체가 차단된다", "Actions 실행분에서는 KOTRA가 계속 실패하고" | 통과 / 불안정 / 차단 세 갈래로 서술 |
| `docs/OPERATIONS.md` | "일부 Azure 러너 IP 대역을 막고 있다" | "전면 차단도 차단 없음도 아니다" + IP별 실측표 6행 |
| `docs/NIGHTLY-No012.md` | "IP 차단이 아니라 복불복", "IP 대역 전면 차단이 아니라" | 선택적·부분적 차단 |
| `scripts/collectors/kotra.py` | "러너 IP 대역 차단으로 보입니다" | "러너 IP에 따른 선택적 차단으로 보입니다(다른 러너에서는 정상 수집되는 실행이 있다)" |

README의 "계속 실패한다"는 서술은 야간 5회 중 3회 성공한 실측과 정반대였다.

## 20. Actions 1차 — run 33817127076 · **success**

러너 IP `68.154.38.20` · 회귀테스트 71건 통과 · **수집원 10/10 정상**
67건 유지 · 장비 45 · ID 100% 생존 · drift 이상 없음 · Telegram 0건
MOFCOM 로마자 폴백 5 → **0건**

## 21. Actions 2차 — run 33818972045 · **success**

러너 IP `172.202.101.246` · 회귀테스트 71건 통과 · 수집원 9/10 (KOTRA 차단 IP)
67건 유지 · 장비 45 · ID 100% 생존 · drift 이상 없음 · Telegram 0건
1차의 번역 회귀 2건 복구 확인

## 22. 기존 공고 재발송 여부: **0건**

2회 실행 모두 "신규 공고 없음". KRISS 판정 변경·제목 번역 변경·URL 정규화
어느 것도 재발송을 일으키지 않았다. 공고 동일성을 URL·ID·제목이 아니라
`announcement_signature()`(출처·**원문** 제목·**원문** 기관·게시일·마감일·
공고유형)로 판단하기 때문이다. `firstSeenAt` 유실 0건, 기존 ID 신규 인식 0건.

## 23. Home 상위 5건

| # | 공고 | 분류 |
|---|---|---|
| 1 | YiSi Wei 패널레벨 패키징 시스템 집적회로 프로젝트 | 🇨🇳 반도체 장비 |
| 2 | 쑤저우 화싱광전 디스플레이 고화질 MINI LED COB 제품 기술 개조 | 🇨🇳 디스플레이 장비 |
| 3 | 반도체 웨이퍼 노광장비 구매_260270 | 🇰🇷 반도체 장비 |
| 4 | [공고 제2026-25호] Large-area MOCVD 1SET 대면적 유기금속화학기상증착장비 | 🇰🇷 반도체 장비 |
| 5 | 청두 BOE 광전 2026년 제4.5세대 TFT-LCD 생산라인 기술 개조 | 🇨🇳 디스플레이 장비 |

전부 반도체·디스플레이 + 실제 장비다. 재료·서비스·공사·일반 연구장비·완제품
없음. KRISS 건은 제외 판정으로 목록에서 빠졌다.

## 24. 운영 사이트

| 항목 | 결과 |
|---|---|
| 홈·공고·통계·일정·설정 | 5개 탭 전부 정상 렌더 |
| 상세 화면 | 정상 (링크 70개 확인) |
| 원문 링크 | 정상, `http://` 링크 **0건** |
| Health 배너 | "수집 장애 1곳"(KOTRA) 정상 표시 |
| 가로 스크롤 (1280px / 375px) | 없음 |
| 모바일 뷰포트 넘침 | 5개 탭 전부 **0개** |
| `undefined` / `NaN` / `[object Object]` | **0건** |

## 25. 콘솔 오류: **0건**

데스크톱·모바일, 5개 탭 이동, 상세 열기 후 재확인 모두 0건.

## 26. 남은 문제

1. **KOTRA — 미해결, 관리 가능.** 러너 IP에 따른 선택적 차단이며 이번 2회
   중 1회 차단됐다. 차단돼도 기존 데이터가 유지되어 화면에서 공고가
   사라지지 않는다.
2. **`YiSi Wei`(奕斯伟) 회사명 1건.** 공식 한국어·영문 표기 근거를 찾지
   못해 그대로 뒀다. 창작하지 않는 것이 규칙이다.
3. **`translationIncomplete` 17건.** 제목 자체는 읽을 만하지만 본문 일부에
   한자·로마자가 남아 배지가 붙는다. 화면에서 "번역 미완료"로 구분되고
   원문 링크가 있어 실무 지장은 없다.

## 27. 다음 우선순위 TOP 3

1. **본문(요약·설명) 번역 품질 점검.** 제목은 이번에 정리됐지만
   `translationIncomplete` 17건의 실제 원인은 본문 쪽이다. 제목과 같은
   방식으로 반복 용어를 추출해 최소 보정하면 배지 수가 줄어든다.
2. **회사명 사전 보강.** `奕斯伟` 등 공식 영문명이 확인되는 중국 반도체
   기업만 골라 `COMPANY_TERMS`에 넣는다. 근거가 확인되는 것만 넣고 추정
   표기는 넣지 않는다.
3. **KOTRA 재시도 횟수 미세 조정 검토.** 지금은 차단 IP를 만나면 즉시
   포기한다(옳다). 다만 "불안정 IP"에서 `RemoteDisconnected`가 5회 이상
   나는 실행이 있어, 목록 요청 재시도를 1회 늘리면 성공률이 오를 여지가
   있다. 실행 환경은 그대로 두는 범위 안의 조정이다.
