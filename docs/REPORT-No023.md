# 완료 보고 — 지시문 No.023

실제 정기 실행 최종 검증 / KAIST 복구 알림 실증 / No.020~No.022 종결 판정

작성 2026-09-05

---

## 1. 결과: PASS

장애 이후 첫 정기 실행에서 검증 항목이 모두 확인됐다.
**No.020 / No.021 / No.022 모두 CLOSED.**

## 2. 확인한 Scheduled Run

| | |
|---|---|
| run id | `33929593732` |
| event | **schedule** |
| 시작 / 종료 | 2026-09-04T23:27:53Z → 23:37:26Z |
| commit | `3613098` |
| 결과 | success |
| RUN_MODE | **scheduled** |
| 회귀테스트 | 217건 PASS |

## 3. KAIST 복구 — 실증 완료

| 항목 | Before | After |
|---|---|---|
| ok | true | true |
| lastStatus | 정상 | 정상 |
| consecutiveFailures | 0 | **0** |
| **failureAlertSent** | **true** | **false** |
| lastRunMode | manual-validation | **scheduled** |
| lastSuccessAt | 09-04T06:34:38Z | **09-04T23:37:15Z** |

수집도 정상이었다 — 3페이지 pagination 정상, 30건 조회 후 최종 1건 반영.

**복구 Telegram: 정확히 1건 발송.** 로그에 `[KAIST] 복구 알림 발송`.
dry-run이 아니라 실제 발송이다.

복구 알림 중복 방지는 `failureAlertSent: false` 로 내려갔으므로 다음
정기 실행에서 0건이어야 한다. 로직은 회귀테스트로 고정돼 있으나
**실측은 다음 정기 실행에서 관찰한다**(지시 7의 관찰 대상).

### KAIST 원인은 여전히 단정하지 않는다

확정된 사실만 적는다 — 서버는 정상(로컬 0.4~1.1초), 타임아웃 12초는
충분, Actions 일부 실행에서만 timeout, 이번 정기 실행에서는 정상.
러너 IP인지 rate limit인지 반복 접근 제한인지 일시 네트워크 문제인지는
**확정되지 않았다.**

## 4. KOTRA — 이번 정기 실행에서 실패

| 항목 | 값 |
|---|---|
| 결과 | 실패 |
| 사유 | 러너 IP에서 `www.kotra.or.kr:443` TCP 연결 불가 |
| consecutiveFailures | 0 → **1** (임계값 3 미만) |
| failureAlertSent | false (장애 알림 없음 — 정상) |
| lastSuccessAt | 09-04T06:34:38Z **유지** |
| 데이터 | 기존 1건 **그대로 보존** |

기존 판정과 일치한다 — "GitHub Actions runner IP에 따른 선택적/부분적
접속 차단". 데이터 손실 0, ID 유지, firstSeenAt 유지, 다른 9개 출처
정상, 기존 공고 재발송 0건.

## 5. 10개 Source 상태

| 출처 | 상태 | 수집 → 반영 |
|---|---|---|
| KANC | 정상 | 12 → 7 |
| NNFC | 정상 | 8 → 3 |
| KOTRA | **실패** | — (기존 1건 유지) |
| EBNEW | 정상 | 21 → 21 |
| MOFCOM | 정상 | 15 → 15 |
| KRISS | 정상 | 3 → 2 |
| JETRO | 정상 | 23 → 18 |
| DGIST | 정상 | 1 → 0 |
| ITRI | 정상 | 1 → 1 |
| KAIST | 정상 | 3 → 1 |

## 6. No.021 수정 — 실운영에서 동작 확인

이번 정기 실행에서 **진행 중 공고 6건이 목록 범위 밖으로 밀렸고 전부
보존됐다.**

```
[MOFCOM] 진행 중 공고 유지: …a03d431bbe60b1  마감 2026-09-16
[MOFCOM] 진행 중 공고 유지: …a03d8e304c2292  마감 2026-09-16
[MOFCOM] 진행 중 공고 유지: …a05ce5dd173952  마감 2026-09-22
[MOFCOM] 진행 중 공고 유지: …a060d4f2c66bf9  마감 2026-09-23
[MOFCOM] 진행 중 공고 유지: …a06643a3806a81  마감 2026-09-24
[MOFCOM] 진행 중 공고 유지: …a0664bde497262  마감 2026-09-24
```

위 두 건은 No.021에서 고친 **지문 충돌 로트**(`…60b1` = /10,
`…04c2292` = 4197-264BOECDCZ02/03)다. 고치기 전이라면 지문이 같은 다른
로트 때문에 버려졌을 항목들이 실제 운영에서 살아남았다.

**Active 공고 소실: 0건.**

`healthy-zero`(정상 수집인데 관련 공고 0건)는 이번 실행에서 **발생하지
않았다** — 실운영 trigger 미발생으로 보고한다(지시 12). 억지로 만들지
않았다. 로컬 10개 출처 검증 PASS 결과는 그대로 유지된다.

## 7. Telegram 분류

| 종류 | 건수 |
|---|---|
| 실제 신규 공고 | **4건** |
| KAIST 복구 | **1건** |
| 기타 출처 복구 | 0건 |
| 출처 장애 | 0건 (KOTRA 연속 1회로 임계값 미만) |
| **사이트 링크 (No.024)** | **1건** |
| 기존 공고 재발송 | **0건** |

신규 4건은 데이터에 새로 추가된 4건과 정확히 일치한다 — **누락 0,
오발송 0.** 전부 MOFCOM 신규 공고이며 새 firstSeenAt(23:37:15)을 받았다.

No.024에서 넣은 사이트 링크가 **첫 정기 실행에서 정상 발송**됐다
(`사이트 링크 알림 발송 완료(1건).`). 신규 4건이 있어도 링크는 1건이고,
모든 알림 뒤 마지막에 나갔다.

## 8. 데이터 무결성

| 항목 | 결과 |
|---|---|
| 건수 | 65 → **69** (추가 4 / 사라짐 0) |
| 기존 ID 유지 | **65/65 (100%)** |
| title / org / dueDate | **0건 변경** |
| equipmentStatus / equipmentReason | **0건 변경** |
| industry / process / firstSeenAt | **0건 변경** |
| 판정 재계산 불일치 | 0건 |
| dueDate 없음 | 9건 유지 |
| id 중복 | 0건 |

## 9. 사이트

전체 69건 / 장비 51 / 제외 18. MOFCOM 15, JETRO 18.
Desktop / Mobile 정상, 콘솔 오류 **0**, 한자·가나 잔여 **0**.

Health 배너에 **KOTRA 장애 1곳**이 표시된다 — 정기 실행에서 실제로
실패했으므로 표시되는 것이 맞다. Manual/Scheduled 배너 분리가 의도대로
동작하고 있다.

## 10. 종결 판정

| 지시문 | 상태 | 근거 |
|---|---|---|
| **No.020** | **CLOSED** | KAIST 정기 복구 + 복구 Telegram 1회 + Manual/Scheduled 분리 정상 + 배너 정상 |
| **No.021** | **CLOSED** | Active 보호 실운영 동작(6건 보존), 소실 0, identity 회귀 0, firstSeenAt 회귀 0 |
| **No.022** | **CLOSED** | 실제 정기 실행 기준 KAIST 복구·Telegram 1회·KOTRA 확인·데이터 안정 |

## 11. 남은 관찰 항목

1. **복구 Telegram 중복 0건** — 다음 정기 실행에서 확인 (플래그는 이미
   내려감)
2. **KOTRA 러너 IP 차단** — 연속 3회가 되면 장애 알림이 나간다. 현재 1회.
3. KAIST / KOTRA 러너 실패 원인 미확정
4. **정기 실행이 매일 크게 지연된다** — cron은 22:00Z(07:00 KST)인데
   실제 실행은 23:27~23:37Z(08:27~08:37 KST). 최근 며칠 +87~98분,
   한 번은 +471분. GitHub Actions 공용 러너의 알려진 동작이며 정각이
   가장 붐빈다. 사용자 판단이 필요한 사항이라 손대지 않았다.

## 12. 다음 단계

**실운영 관찰 모드로 전환한다.** 새 기능을 만들지 않고, 정기 실행을
계속 돌리면서 공고 소실 / 신규 알림 누락 / 중복 / Health 장애 / 번역
이상 / 분류 이상이 실제로 발생할 때만 다음 작업을 만든다.
