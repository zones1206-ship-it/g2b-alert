# 지시문 No.016 완료 보고

작업일: 2026-09-04 · baseline `2e59d5b` → 최종 `main`

---

## 1. 작업 결과: **PASS**

성공 기준 21개를 모두 충족했다.

## 2~5. 사라진 공고와 시점

| 항목 | 값 |
|---|---|
| **ID** | `ebnew709582888` |
| **공고명** | 반도체 장비 2차 국제 입찰공고 (1) / 半导体设备第二批国际招标公告(1) |
| **출처** | EBNEW (중국 비롄왕) |
| **사라진 시점** | **2026-09-04 00:28 UTC** — No.014의 Actions 1차(`917172a`) |
| 게시일 / 마감일 | 2026-08-20 / **2026-09-11** |
| firstSeenAt | 2026-08-20T22:21:07+00:00 |

같은 실행에서 `jetro401416`도 빠졌으나 그건 상세 503이 원인이었고
No.014에서 이미 복원했다. 남은 1건이 45→44의 원인이다.

## 6. 감소 원인

> **45→44 감소 원인: EBNEW 수집기의 14일 조회 창(`LOOKBACK_DAYS`)을 게시일이
> 벗어나 제외된 것. 원본 공고는 삭제되지 않았고 마감(2026-09-11)도 남아 있었다.**

```python
cutoff = (오늘 - 14일)
if row["date"] < cutoff:   # 게시일 2026-08-20
    break                  # 09-03 컷오프 08-20 → 통과 / 09-04 컷오프 08-21 → 제외
```

지시문 3번의 A~G 어디에도 정확히 맞지 않는 **H(기타)**다.

## 7. 실제 원본 삭제 여부: **아니오**

상세 페이지를 직접 열어 확인했다. 제목이 그대로 있고
`投标截止时间（开标时间）: 2026-09-11 10:00`으로 마감도 남아 있다.
지금 `半导体设备` 검색 **2페이지 2번째**에 있으며 수집 범위(1~2페이지) 안이다.

## 8. 시스템 누락 여부: **예**

- 상세 요청 실패가 아니다 — 실행 로그에 `709582888` 문자열 자체가 없다.
- 목록 수집 실패가 아니다 — EBNEW는 정상 수집됐다(raw 35건).
- 수집기 자신의 조회 창이 원인이다.

## 9. 최종 복원 여부: **복원함**

원래 `firstSeenAt`(2026-08-20T22:21:07+00:00) 그대로 되살렸다. 그래야 다음
실행에 신규로 오인되어 Telegram이 다시 나가지 않는다.

**규칙도 고쳤다** — 게시일이 조회 창 밖이어도 마감일이 남아 있으면 버리지
않는다(`is_still_open`). 마감일을 못 읽으면 붙들지 않아 오래된 공고가 쌓이지
않는다. 검색 결과 전수 확인 결과 이 규칙으로 늘어나는 것은 **2건뿐**이다
(잃어버린 1건 + 계속 놓치고 있던 1건). 나머지 12건은 이미 마감이다.

## 10~11. 최종 건수

| | No.015 종료 | No.016 종료 |
|---|---|---|
| 전체 | 66건 | **69건** |
| 장비 | 44건 | **47건** |
| 검토 필요 | 0건 | **0건** |
| 제외 | 22건 | **22건** |

복원 1건 + 신규 유입 3건(ITRI 1 · EBNEW 1 · KANC 1) − KAIST 1건 이탈 = +3.
KAIST 이탈은 아래 41절 참조.

## 12. 6개 수집기 기존 위험 구조

| 수집기 | 안정 ID | 상세 실패 시(기존) | 위험 |
|---|---|---|---|
| KANC | `cid{cid}` | `continue` → 소실 | 있음 |
| NNFC | `nttid{ntt_id}` | `continue` → 소실 | 있음 |
| KRISS | `kriss{list_no}` | `continue` → 소실 | 있음 |
| KOTRA | `kotra{biz_no}` | `continue` → 소실 | 있음 |
| EBNEW | `ebnew{숫자}` | `build_item`이 `None` → 소실 | 있음 |
| MOFCOM | `mofcom{fdid}` | `build_item`이 `None` → 소실 | 있음 |

EBNEW·MOFCOM은 상세 실패가 `None`으로 돌아와 **"장비 공고가 아니라
걸러냈다"(정상)와 구분되지 않았다.**

## 13~18. 수집기별 수정 결과

공통 도구를 `collectors/common.py`에 뒀다(지시문 20번).

```python
class DetailFetchFailed(RuntimeError)   # '장비 아님'과 구분하는 신호
def previous_index(previous_items)      # 직전 결과 id 조회용
def keep_or_defer(prev_index, id, tag, exc)
```

| 수집기 | 수정 | 결과 |
|---|---|---|
| **KANC** | `except` 블록 교체 | 상세 실패 시 기존 공고 유지. 장비 필터 무변경 |
| **NNFC** | `except` 블록 교체 | 〃 (기관명만으로 장비 인정 않는 규칙 유지) |
| **KRISS** | `except` 블록 교체 | 〃 (일반 연구·계측 장비 제외 규칙 유지) |
| **KOTRA** | `except` 블록 교체 | 〃 (endpoint·실행환경 무변경, No.010 구조 유지) |
| **EBNEW** | `None` → `DetailFetchFailed` + 호출부 구분 | canonical https URL·id·firstSeenAt·번역 결과 모두 유지 |
| **MOFCOM** | 〃 | fingerprint(fdid)·originalUrl·firstSeenAt·dedupe 유지 |

`fetch_announcements.run_collector`가 `PREVIOUS_ITEMS`를 선언한 수집기에만
직전 결과를 넘긴다. 나머지(DGIST·ITRI·KAIST) 동작은 그대로다.

## 19~21. 보존·firstSeenAt·복귀 테스트

| 항목 | 방식 | 결과 |
|---|---|---|
| 상세 실패 시 기존 item 보존 | JETRO CASE B를 실제 collect() 모의로 검증 + 6곳은 배선 고정 | PASS |
| firstSeenAt 유지 | JETRO CASE C (Run A→B→C) | PASS |
| 복귀 후 Telegram | JETRO CASE D — 중간 실행에서 빠지지 않아 신규로 안 잡힘 | PASS |
| 부분 실패 23건 중 1건 | JETRO — 23건 유지 | PASS |

6개 수집기는 구조가 제각각이라 CASE A~G를 각각 모의하기 어려워, **보존이
가능한 배선 상태**를 코드 기준으로 고정했다(`PREVIOUS_ITEMS` 선언,
`keep_or_defer` 사용, 옛 `실패(건너뜀)` 패턴 부재). JETRO는 collect()를 직접
모의해 CASE A~G 전부를 검증한다.

## 22. 신규 ID + 상세 실패

유지할 기존 데이터가 없으므로 **상세를 지어내지 않고 다음 실행으로 미룬다.**
로그에 `신규라 보류 N건`으로 남는다.

## 23. JETRO 회귀 결과

No.015의 CASE A~G가 그대로 통과한다(테스트 9건). 이번 수정으로 깨지지 않았다.

## 24~26. 변경 건수

| 항목 | 결과 |
|---|---|
| 기존 ID 변경 | **0건** |
| 기존 firstSeenAt 변경 | **0건** |
| 기존 분류(equipmentStatus/Reason) 변경 | **0건** |
| industry / process / equipmentName | **0건** |

## 27. 외국어 잔여 실제 화면 Before / After

배포 사이트에서 **화면에 실제로 보이는 텍스트만** 추출해 검사했다.

| 항목 | Before | After |
|---|---|---|
| 한자 | 0건 | **0건** |
| 가나 | 0건 | **0건** |
| 금지 영어 UI 문구 | 0건 | **0건** |
| 로마자 | 4종 | **1종** (`DaTai`) |

No.015 보고의 "한자 0건 vs 잔여 4건"은 모순이 아니었다 — 한자 원문은
`original*` 필드에만 있고(유형 A) 화면에는 그 **로마자 표기**가 보인다.
보고서가 잔여를 한자로 적어 오해를 샀다. 이번에 유형별로 분리해 정리했다.

## 28. `大台` 처리: **유지**

공개 원문(EBNEW 상세)에도 설명이 없다. 뜻을 지어내지 않는다.
`"원문 표현 확인 필요"` 같은 대체 문구도 검토했으나 택하지 않았다 —
현재 표시 `DaTai 레이저 현미경`은 **장비 종류가 읽히는데**, 안내 문구로
바꾸면 정보가 오히려 줄어든다. 번역 미완료 배지와 원문 링크가 이미 있다.

## 29. `奕斯伟` 처리: **이쓰웨이(ESWIN)**

회사 자체 도메인 `eswin.com`에서 `北京奕斯伟科技集团有限公司-官网`으로
공식 영문명을 확인했다. 한국어는 뜻을 지어낸 것이 아니라 발음 음역이며
사전의 다른 회사와 같은 방식이다(京东方 → 징둥팡, 华星光电 → 화싱광전).

## 30. `奕成` 처리: **이청**

공식 영문명을 확인하지 못해 음역만 사용했다.

## 31. `电子` 처리: **전자**

일반 기술용어이며 회사명 접미사로 흔히 쓰인다(`…电子有限公司`).
`电子材料`·`微电子`·`光电子`를 함께 넣어 긴 조합어가 먼저 치환되게 했다.

## 32. 허용 고유명사 / 약어

`scripts/check_ui_language.py`의 `ALLOWED_WORDS`로 관리한다.

- 공정·장비: AOI, ALD, CVD, PVD, PECVD, MOCVD, CMP, TGV, RTP, ECD, RIE,
  DRIE, LED, OLED, AMOLED, TFT, TFT-LCD, LCD, CMOS, SOI, MRAM, SEM, TEM
- 기관: KOTRA, JETRO, ITRI, DGIST, KAIST, KANC, NNFC, KRISS, EBNEW, MOFCOM,
  RIKEN, AIST, JAEA, NIMS, QST, JAXA, NEDO, NICT, BOE, TCL, CSOT
- 허용 고유명사(화면 노출): `DaTai`(확인 불가), `Xinlian Microelectronics`·
  `Shenyang Railway Signal`·`Shennan Circuits`·`Visionox`·`ESWIN`(공식 영문명 병기)

## 33. UI 외국어 자동검사 결과

**정확했다.** 이번에 실제로 결함을 하나 잡았다 — 34절 참조.
검사 범위도 확인했다: `original*` 필드는 검사하지 않아 false positive가
없고, 표준 약어는 허용 목록으로 걸러 과탐이 없다.

## 34. 자동 테스트

**122건 / PASS 122 / FAIL 0** (102건 → 122건, +20건)

추가: 상세 실패 보존 공용 도구(5건), 7개 수집기 배선 고정(4건),
EBNEW 조회 창(5건), ITRI 번체 용어집(6건).

## 35. Actions 1차 — run 33828796113 · **success**

러너 IP `68.154.117.43` · 회귀테스트 116건 통과 · **수집원 10/10 정상**
67 → 70건 · 장비 45 → 48 · **ID 100% 유지** · drift 이상 없음
Telegram 3건 — 전부 실제 신규(ITRI 첫 수집 건, EBNEW 조회창 복구분,
KANC 당일 신규). 재발송 0건.

## 36. Actions 2차

**1회차 failure → 원인 수정 후 재실행 success (run 33830665428)**

2차가 회귀테스트 단계에서 멈췄다. UI 외국어 검사가
`itriA512004207-10.contractMethod = "견적 요청(詢價案)"`을 잡았다.
사용자에게 그대로 보이는 값에 한자가 들어 있었다.

**보호장치가 설계대로 동작한 것이다** — 수집·Telegram·커밋 **이전** 단계에서
멈춰 잘못된 데이터가 나가지 않았다. 이 한자는 검사 도입 전부터 코드에
있었지만 ITRI가 계속 0건이라 데이터에 나타난 적이 없었고, 첫 공고와 함께
드러났다.

재실행 결과: 70건 유지 · 장비 48 · **ID 100%** · Telegram 0건 · drift 이상 없음.

이후 회귀 하나를 더 잡아 확인 실행을 한 번 더 돌렸다(run 33831518871,
success) — 41절 참조.

## 37. Telegram 재발송: **0건**

복원·보존 구조 변경·번역 변경 어느 것도 재발송을 일으키지 않았다.
1차의 3건은 실제 신규 공고다.

## 38. Health 상태

최종 실행 기준 **수집원 10곳 전부 정상**. 장애 배너 없음.

## 39. Desktop / Mobile

| 항목 | Desktop(1280px) | Mobile(375px) |
|---|---|---|
| 5개 탭 | 정상 | 정상 |
| 가로 스크롤 | 없음 | 없음 |
| 뷰포트 넘침 | 0개 | 0개 |
| 상세 열기 | 정상 | 정상 (원문 블록 없음, 한자 없음) |
| 화면 한자·가나 | 0건 | 0건 |
| 원문 링크 | `중국 비롄왕(EBNEW) 원문 공고 보기` | 정상 |

## 40. 콘솔 오류: **0건**

## 41. 남은 문제

1. **KAIST 공고가 목록에서 밀려 사라진다 — EBNEW와 같은 유형.**
   확인 실행에서 `반도체 웨이퍼 노광장비 구매_260270`(게시 08-31,
   **마감 09-08로 아직 진행 중**)이 빠졌다. KAIST는 `LIST_URL` 한 페이지
   10건만 읽고 페이징 파라미터가 없어, 새 공고 10건이 올라오면 밀려난다.
   1~2줄 수정으로 해결되지 않아(사이트 페이징 구조 파악 필요) 이번에는
   손대지 않았다. **같은 위험이 다른 게시판형 수집원에도 있을 수 있다.**
2. **`大台` 1건** — 확인 불가라 유지.
3. **KOTRA 러너 IP 선택적 차단** — 변동 없음.
4. **6개 수집기 CASE A~G 개별 모의 미구현** — 구조가 제각각이라 배선
   고정으로 대신했다. JETRO만 collect() 직접 모의로 전 케이스를 검증한다.

## 42. 다음 우선순위 TOP 3

1. **수집 창(window) 때문에 진행 중 공고를 놓치는 문제 일괄 점검.**
   이번에 EBNEW(날짜 창)와 KAIST(페이지 깊이) 두 유형이 확인됐다.
   10개 수집기 전부에 대해 "마감 전 공고가 우리 수집 범위 밖으로 밀려나는
   조건"을 표로 만들고, 마감일 기준 보호를 넣을 수 있는 곳부터 적용한다.
   **45→44의 진짜 교훈이 여기에 있다.**
2. **6개 수집기 CASE B/C 실측 테스트.** 지금은 배선만 고정돼 있다.
   각 수집기의 `collect()`를 모의해 상세 실패 시 실제로 기존 공고가
   유지되는지 확인하면 보존 구조를 확실히 못 박을 수 있다.
3. **중국 반도체 기업 회사명 사전 보강.** 이번에 `奕斯伟`(ESWIN)를 공식
   사이트로 확인해 넣었다. 같은 방식으로 확인 가능한 기업만 계속 넣는다.
   확인되지 않으면 지금처럼 로마자를 유지한다.

---

## 부록. 작업 중 잡은 회귀 2건

**(1) ITRI `contractMethod`의 한자** — 위 36절. UI 검사가 잡았다.

**(2) 회사명이 쪼개진 것.** `微电子 → 마이크로전자`를 일반 용어로 넣었더니
회사명 `芯联微电子`가 `芯联` + `마이크로전자`로 쪼개졌다.

```
이전: 충칭 신롄 마이크로전자(Xinlian Microelectronics) 유한공사 전공정 …
회귀: 충칭 XinLian 마이크로전자 유한공사 QianDuan …
```

회사명이 `zh_ko_argos`에만 있어 폴백 경로(`zh_translate` 용어집)에서
쪼개졌다. `COMPANY_TERMS`(가장 먼저 치환)에도 넣어 통째로 유지되게 했고,
`前段`/`后段`(전공정/후공정)도 함께 넣었다. 확인 실행에서 복구를 확인했다.

두 회귀 모두 **자동 검사와 실제 실행이 잡아냈다.** 코드 리뷰만으로는
드러나지 않았을 것이다.
