# 완료 보고 — 지시문 No.022

KAIST 정기 복구 알림 실증 / 복구 중복 방지 / No.021 소실 방지 구조
실운영 최종 검증

작성 2026-09-04

---

## 1. 결과: PARTIAL

**코드 문제가 아니라 시간 때문이다.** KAIST 정기 복구 알림을 검증하려면
정기 실행이 필요한데, 다음 정기 실행이 아직 오지 않았다.

지시 3·26에 따라 수동 실행을 정기 실행처럼 위장하지 않았고, Health JSON도
손대지 않았으며, `failureAlertSent` 를 강제로 지우지 않았다. 그 항목만
PENDING으로 둔다. 나머지는 전부 검증했다.

**이번 작업에서 코드와 데이터를 한 줄도 바꾸지 않았다.**

## 2. 정기 실행 확인 결과

`event_name == schedule` 인 실행은 하나뿐이다.

| | |
|---|---|
| run id | `33818245127` |
| 시각 | 2026-09-03T23:35:21Z |
| 결과 | success |

**이 실행은 KAIST 장애가 생기기 전이다.** KAIST가 `failureAlertSent: true`
가 된 것은 2026-09-04T05:29Z 이후이므로, 이 정기 실행으로는 복구를 검증할
수 없다. 그 실행 자체는 67건 / ID 유지 100% / 이상 없음으로 정상이었고,
장애·복구·신규 알림 모두 0건이었다.

현재 시각 07:12Z, 다음 정기 실행 22:00Z — 약 15시간 뒤다.

## 3. Baseline

전체 65건 / 장비 47 / 제외 18 / 검토 필요 0.
EBNEW 21, JETRO 18, MOFCOM 11, KANC 7, NNFC 3, KRISS 2, ITRI 1,
KAIST 1, KOTRA 1. dueDate 없음 9건.

10개 출처 모두 `ok: true`, 연속 실패 0. **KAIST만 `failureAlertSent: true`**
로 복구 알림을 기다리고 있다.

## 4. PENDING — KAIST 정기 복구

| 항목 | 상태 |
|---|---|
| consecutiveFailures | 0 (이미 초기화됨) |
| failureAlertSent | **true** (복구 알림 대기) |
| lastRunMode | manual-validation |
| 다음 정기 실행에서 나갈 알림 | KAIST 복구 **1건** (dry-run 확인) |
| 실제 발송 확인 | **PENDING** |

지시 5대로 dry-run만으로 PASS 처리하지 않는다.

복구 알림 중복 방지(지시 6)도 그 다음 정기 실행이 필요하므로 PENDING이다.
다만 로직 자체는 회귀테스트로 고정돼 있다 — 복구 알림을 4회 연속 정상
실행에 걸쳐 돌려도 1회만 나간다.

### KAIST 원인은 여전히 단정하지 않는다

확정된 사실만 적는다 — 서버는 로컬에서 정상(0.4~1.1초), 타임아웃 12초는
충분, Actions 일부 실행에서만 timeout, 간격을 둔 뒤에는 정상. 러너 IP인지
rate limit인지 반복 접근 제한인지 일시 네트워크 문제인지는 **확정되지
않았다.**

## 5. KOTRA

현재 `ok: true`, 연속 실패 0, `failureAlertSent: false`, 1건 보유.
기존 판정을 유지한다 — "GitHub Actions runner IP에 따른 선택적/부분적
접속 차단". 정기 실행 기준 상태 확인은 KAIST와 함께 PENDING이다.

## 6. Active 보호 — 실제 데이터로 10개 출처 전수 검증

정상 수집인데 관련 공고 0건인 상황을 실제 보유 데이터로 재현했다.

| 출처 | 진행 중 | 유지 | 소실 |
|---|---|---|---|
| KANC | 7 | 7 | 0 |
| NNFC | 3 | 3 | 0 |
| KOTRA (KEEP) | 0 | 1 | 0 |
| EBNEW (KEEP) | 12 | 21 | 0 |
| MOFCOM (KEEP) | 11 | 11 | 0 |
| KRISS | 2 | 2 | 0 |
| JETRO | 18 | 18 | 0 |
| DGIST | 1(합성) | 1 | 0 |
| ITRI | 1 | 1 | 0 |
| KAIST | 1 | 1 | 0 |

**소실 0건.** No.021에서 고친 healthy-zero 경로가 실제 데이터에서도
동작한다. 다만 이 경로가 **실운영 정기 실행 로그에서 관측된 것은 아니다**
— No.021 수정 이후 Actions를 한 번도 돌리지 않았기 때문이다(지시 22).

## 7. 정책 회귀 — 실제 데이터 기준

| 지시 | 검증 | 결과 |
|---|---|---|
| 12 | 사라졌다 돌아온 공고 4건 | 유지 ✓ / firstSeenAt 유지 ✓ / 신규 알림 **0건** |
| 13 | 진짜 신규 로트 | 신규 알림 **1건** / 새 firstSeenAt 부여 ✓ |
| 14 | true reindex | 신규 알림 **0건** / 재색인 1 / firstSeenAt 승계 ✓ |
| 15 | 동일 projectNo 정정공고 | 원공고 유지 ✓ (11건 전부 보존) |
| 18 | Manual 실패 | 연속 실패 2→2(증가 없음), 알림 차단, 배너 제외 ✓ |
| 18 | Scheduled 실패 | 연속 실패 2→3 (정상 누적) ✓ |
| 18 | 신규공고 알림 | RUN_MODE 참조 없음 — 정책 그대로 ✓ |

## 8. firstSeenAt 전수 sanity check

| 검사 | 결과 |
|---|---|
| 기록보다 늦게 찍힌 값 (의도적 정정 제외) | **0건** |
| 지문 같은 별개 로트가 값을 공유 | 1건 → **정상** |
| firstSeenAt 누락 | 0건 |
| id 중복 | 0건 |

공유 1건은 JETRO `402047` / `402048` 이다. git 이력을 보면 **같은 실행
(2026-09-03T10:45:54+09:00)에서 함께 처음 발견됐다.** 잘못된 상속이
아니라 실제로 같은 시각에 들어온 것이다. 되돌리지 않았다.

No.020에서 의도적으로 정정한 `mofcom…816a91` 도 그대로 두었다.

## 9. 데이터 무결성

| 항목 | 변경 |
|---|---|
| 건수 (65) | 추가 0 / 사라짐 0 |
| id / title / org / dueDate | 0건 |
| equipmentStatus / equipmentReason | 0건 |
| industry / process / firstSeenAt | 0건 |

판정 재계산 불일치 0건, dueDate 없음 9건 유지.

## 10. 테스트

**203건 전체 PASS.** 지시 21이 지정한 필수 항목 53건 포함.

## 11. 사이트

Desktop / Mobile 정상. Health 배너 **없음**(KAIST·KOTRA 모두 정상).
유효 공고 47, 제외 18, MOFCOM 11, 일정 탭 정상 렌더, 한자·가나 잔여 0,
**콘솔 오류 0**.

NEW 배지 31건은 최근 48시간 내 수집분이며, No.021에서 복구한 EBNEW 건의
잘못된 배지는 사라진 상태다.

## 12. Actions 실행

**0회.** 기존 실행 결과만 사용했다(지시 22).

## 13. 종결 판단

| | 상태 |
|---|---|
| No.020 | **PENDING 유지** — KAIST 복구 알림 실증이 조건 |
| No.021 | 코드·회귀는 전부 정상이나, 실운영 정기 실행 관측이 남아 **PENDING 유지** |

둘 다 다음 정기 실행 한 번으로 함께 닫을 수 있다.

## 14. 남은 문제

1. KAIST 정기 복구 알림 실제 발송 확인 (22:00Z 이후)
2. 복구 알림 중복 0건 확인 (그 다음 정기 실행)
3. KAIST / KOTRA의 Actions 러너 실패 원인 미확정

## 15. 다음 우선순위

1. 다음 정기 실행 결과 확인 — KAIST 복구 1건, No.021 수정의 실운영 동작
2. 그 다음 정기 실행에서 복구 중복 0건 확인 → No.020·No.021 종결
3. 러너 실패 통계 축적
